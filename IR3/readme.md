```
All modules can be ran without parameters to show the usage.

The searcher searches for the token on multiple documents wich are not included within this repo as they are too big.  Only a small example is included: text.csv.

Usage example:
python3 Ranker.py query
Note: Default parameters are set such as min_lenght, stopwords and stemming.

Didn't manage to implement BM25 since i did restructure the way the index are saved, loaded, indexed and merged.

```